# Reuse of software - Final Project

### To run:
- run ```nix develop``` inside ./env/
- then run ```node ./database/migrate``` to create the db (run ```node ./database/reset``` if migration fails)
- after that just run ```node start``` or ```nodemon start``` to run the server
